@model IEnumerable<IIT.EResult.Models.TeacherModel>

@{
    ViewBag.Title = "Teacher Summary";
}

<h2>Teacher Summary</h2>

<p>@Html.ActionLink("Create New", "Create", "Teacher")</p>
<div id="message" class="message-success">
    @if (ViewBag.Mode)
    {
        @ViewBag.Message
    }
</div>
<div>
    <table>
        <tr style="background-color: gray;">
            <th>Name</th>
            <th>Designation</th>
            <th>Email</th>
            <th>Contact No</th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                <td>@Html.DisplayFor(modelItem => item.Designation)</td>
                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                <td>@Html.DisplayFor(modelItem => item.ContactNo)</td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    </table>
</div>
<div>
    @*<script type="text/javascript">
        $(document).ready(function () {
            if ($("#message:contains('Successfully')")) {
                $("#message").addClass('message-success');
            }
            if ($("#message:contains('Failed')")) {
                $("#message").addClass('message-error');
            }
            if ($("#message").val().length > 0) {
                $("#message").hide(slow);
            }
        });
    </script>*@
</div>
