@model IEnumerable<IIT.EResult.Models.StudentModel>

@{
    ViewBag.Title = "Student Summary";
}

<h2>Student Summary</h2>

<p>@Html.ActionLink("Create New", "Create", "Student")</p>
<div id="message" class="message-success">
    @if (ViewBag.Mode)
    {
        @ViewBag.Message
    }
</div>
<div>
    <table>
        <tr style="background-color: gray;">
            @*<th>Id</th>*@
            <th>Name</th>
            <th>Batch No</th>
            <th>Session</th>
            <th>Email</th>
            <th>Contact No</th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                @*<td>@Html.DisplayFor(modelItem => item.Id)</td>*@
                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                <td>@Html.DisplayFor(modelItem => item.BatchNo)</td>
                <td>@Html.DisplayFor(modelItem => item.Session)</td>
                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                <td>@Html.DisplayFor(modelItem => item.ContactNo)</td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    </table>
</div>
<div>
    @*<script type="text/javascript">
        $(document).ready(function () {
            if ($("#message:contains('Successfully')")) {
                $("#message").addClass('message-success');
            }
            if ($("#message:contains('Failed')")) {
                $("#message").addClass('message-error');
            }
            if ($("#message").val().length > 0) {
                $("#message").hide(slow);
            }
        });
    </script>*@
</div>
